# メタモデルと検査規則の設計判断

README の[メタモデル](../../README.md#メタモデル)と[検証 (3層)](../../README.md#検証-3層)が
規則そのものを書くのに対し、ここは**なぜその規則なのか**を残す。
規則を変えるとき、同じ議論を繰り返さないための文書。

## 語尾規則を指示書より緩く取った

初回の実装指示書 (issue #1) の記述をそのまま条件式にすると実務的に破綻する箇所が
2 つあり、意図を汲んで次のように解釈した。

- **Need の語尾規則**: 指示書は「〜したい」。ただし「気づきたい」「知りたい」のような
  サ変以外の願望形を弾いてしまうため、**「〜たい」**で判定する。
- **FR の語尾規則**: 指示書は「〜すること」。同様に「読み取ること」「送ること」を弾くため、
  **「〜こと」**で判定する。

いずれも末尾の句点は許容する。厳密に「〜したい」「〜すること」に戻すなら
`src/reqmodel/definition/nodes.py` の `_check_suffix` 2 か所を変えるだけでよい。

## `constrains` を `structure.status_inconsistent` の対象から外した

`structure.status_inconsistent` が見るのは `satisfies` / `refines` / `qualifies` /
`motivates` の 4 種で、**`constrains` は対象外**である (`validate.py` の `_STATUS_EDGES`)。

制約は制約対象より先に決まりうる。「MCP サーバを作るなら依存を増やさない範囲で」は
着手前に決まっているからこそ意味があり、承認済みの Constraint が proposed の要求を
指すのは成熟度の逆転ではない。制約側の status を下げれば黙るが、それは実態に反する。

これは[このリポジトリ自身の要求モデル](../../README.md#このリポジトリ自身の要求モデル-dogfooding)を
`--strict` に通す過程で見つかった誤検出である。approved な Constraint が proposed な
FR を `constrains` した結果が報告され、抑制ではなく検査そのものを直した (#47)。

## status を合意状態と実現状態に分けた

単一の `Status` を全ノードへ持たせる設計をやめ、Goal / Need / Constraint の
`DecisionStatus` (`proposed` / `approved`) と、FR / QR の `RequirementStatus`
(`proposed` / `approved` / `implemented` / `verified`) に分けた (#72)。フィールドを
二つに分けるのではなく型ごとの許容値を変えたのは、ひとつの要求について状態を一方向に
進める使い方を保ちつつ、Goal の「実装済み」や Constraint の「検証済み」という機械が
正誤を判定できない記述を入力時に拒否するためである。

`structure.status_inconsistent` は実現状態の比較ではなく、**合意済みの構造が未合意の
ノードに依存していないこと**を検査する。したがって `verified` の QR から `approved` の
FR への `qualifies` は正しい。参照先にも同じ status を要求すると、`implemented` を持たない
Need を implemented の FR が満たす正常な構造を表現できない。この規則では参照元が
`approved` 以上なら参照先が `approved` であることだけを求める。

## `Decision` 型と `conflicts` / `resolves` を置かない

対立の明示 (`conflicts`)、その解消の記録 (`Decision` / `resolves`)、そして
`structure.conflict_unresolved` / `structure.resolve_no_conflict` /
`structure.resolve_invalid` の 3 チェックをメタモデルから外した。決定の内容は
**要求 (FR) として書く**。

分かれ目は**決定の中身が何の形をしているか**にある。外した時点でモデルに存在した
`Decision` は 2 件で、どちらも対立する要求のどちらかを選んだのではなく、新しい
仕組みを持ち出して両立させていた (「規程照合に要する項目を既定値から自動補完する」
「選択したノードの近傍だけを描くフォーカス表示を設ける」)。主語はシステムで、
消すと機能が減る。[どの型に書くか](../../README.md#どの型に書くか-主語で決める)を
当てれば、これは FR である。

FR の中身が `Decision` に書かれていると、FR に課される規則をすべて回避する。
語尾規則がかからず、`acceptance_criteria` を持てず、`Decision` は通常の要求ノードでは
ないので外部参照も持てず、`structure.orphan_fr` の対象外で、
`req doc --matrix` のトレーサビリティ表にも出ない。実際 `requirements.py` では
「フォーカス表示を設けること」が FR として存在せず、この機能が現れるのは D-1 の
`text` と QR-1 の受け入れ基準 (「フォーカス 2 ホップ表示の倍率が 0.5 倍以上」) の
中だけだった。**解決策は既に片方の要求の受け入れ基準へ漏れ出しており**、
「決定は `Decision` に一元化されている」という前提は先に崩れていた。

**捨てたもの**: 「対立に気づいているが、まだ決めていない」という**開いた状態**。
FR に畳んだ後の text は決着した姿しか表さず、テキストには状態も検査も持てないので
これは復元できない。ただしこれは構造上の事実ではなく進行中のプロセスの状態であり、
「人・時点・経緯は Git、構造はモデル」という既存の分担 (README の
「Git を履歴・レビュー基盤とする」) の前者側にあたる。
決着すれば解決策が FR として残り、揉めた経緯は commit と PR が持つ。

**この判断はプロジェクトの性格に依存する。** 複数の当事者が交渉する場では、
未解決の対立を一覧に出して会議を強制すること自体に価値があり、そこでは
`conflicts` を残す意味がある。このリポジトリは書き手が 1 人で、対立が open な
まま残る期間がそもそも無いため、検査が働く余地がなかった。書き手が増えて
「決まっていないことを機械に数えさせたい」場面が出てきたら、ここを読み直して
戻す判断をしてよい。

`Decision` が通常の要求ノードではない非対称は以前から気になっていた点だが、答えは
「外部参照フィールドを足す」ではなく「`Decision` が要らない」だった。中身が FR なら
`source` も語尾規則も受け入れ基準も最初から付いてくる。

## 優先度を属性として持たない

`Node.priority` と `HIGH_PRIORITY_THRESHOLD` をメタモデルから外した。
**優先順位付けは時点・関係者・基準に依存する活動であり、モデルは現時点の
構造上の事実だけを持つ**。順序を永続化したいなら issue / milestone / PR の担当とする。

これは 1 つ上の `conflicts` / `Decision` と**同じ基準の適用**である。3 つは
「まだ決めていないこと」を表す点で同列だった。

| 概念 | 何を表すか |
|---|---|
| `conflicts` | 判断が必要な**場** |
| `Decision` | 判断の**記録** |
| `priority` | 判断の**材料** |

場と記録を外して材料だけ残すと、優先度を使う場所がモデルの外にしか無いのに
値だけモデルに置く、という中途半端な状態になる。

**もともと priority は、このモデルで唯一「間違った値」を定義できない属性だった。**
語尾規則・エッジ型規則・waiver の理由必須・受け入れ基準の有無まで機械検査している
なかで、ここにだけ規則が無い。実質的な用途は `validate.py` の 1 行
(`structure.conflict_unresolved` の severity を対立するノードの優先度で上げ下げする)
だけで、これは `conflicts` と一緒に消えた。外した時点で priority は検査規則を 1 つも
持たない表示専用フィールドになっていた。

要求工学の側から見ると、優先度は ISO/IEC/IEEE 29148 の要求属性に挙がっている。
用途は (a) 対立の解消と (b) スコープ交渉・リリース計画の 2 つで、(a) は
`conflicts` を外した時点で場そのものが無くなった。(b) は対立が 1 件も無くても
行う活動なので独立だが、**リリース計画は「これから何を作るか」という将来の計画で
あって現時点の事実ではない**ため、同じ理由で外に出す。そもそも優先度は
〈ある時点・ある関係者・ある基準〉のもとで一組の要求に相対的に付く値であり、
要求に固有の性質ではない。IREB が prioritization を属性ではなく**活動**として
扱うのはそのためである。

外した時点でモデルにあった値は、この見方を裏づけていた。`requirements.py` では
Constraint 5 件にも priority が入っていたが、**制約は交渉対象外だから制約**であり、
優先度を比べて片方を落とす対象ではない。値が埋まっていたのは構造上必要だったから
ではなく、`Node` 基底にフィールドがあったからである。

**捨てたもの**は棚卸しの順序 (テーブルの「どれから潰すか」) と、サイトの視覚軸を
1 本 (枠の外側の輪)。ただしテーブルには**指摘件数**の列があり、こちらは手で維持
しない導出値なので、順序付けとしてはむしろ健全である。status も成熟度順に並ぶ。

**この判断もプロジェクトの性格に依存する。** 一度に着手できる量よりはるかに多くの
要求を抱え、リリースの線引きをモデルの中で数えさせたい規模になったら、ここを
読み直して戻す判断をしてよい。戻すなら、表示専用フィールドとしてではなく
「間違った値」を定義できる形 (例えば同じ Need を満たす FR 群の中で順序が一意か)
で入れること。

## 検証可能性を evidence 側に置く

`structure.missing_acceptance_criteria` (受け入れ基準の無い FR / QR を warning) を
外し、代わりに `evidence` を FR / QR に足して `structure.unverified_claim`
(`verified` なのに `evidence` が無い) を置いた。`acceptance_criteria` は任意属性として
残す。**「何を受け入れるか」(事前の約束) から「なぜこれで OK としたか」(事後の根拠) へ
中心を移した**ことになる。

### 要求工学の側

ISO/IEC/IEEE 29148 が要求 1 件ごとに課す品質特性に `verifiable` は入っているが、
要求しているのは**要求文そのものが検証可能であること**であって、別立ての受け入れ基準欄
ではない。INCOSE はむしろ分けており、検証方法 (I/A/D/T) と検証結果は要求の属性ではなく
検証トレーサビリティ表の側に持つ。「Acceptance Criteria」はアジャイルのユーザーストーリーと
契約の受入テストの語彙で、このリポジトリが折衷元に挙げる IREB / INCOSE のどちらの
メタモデルの中心概念でもない。

### 実測が示していたこと

要求文が測定可能に書けているとき、受け入れ基準は要求文の再掲になる。外す前の QR-2 が
それだった。

```
text: 300 ノードの定義ファイルについて、読み取りから指摘の出力までを 2 秒以内で終えること
AC  : examples/bench.py に対する req validate が 2 秒以内に終わる
```

AC が text に足しているのは「2 秒」ではなく `examples/bench.py`、つまり**どこで
確かめたか**である。これは基準ではなく根拠で、置き場所が無いために AC へ流れ込んでいた。
QR 4 件・AC 6 行のうち text に無い基準を足していたのは QR-4 の 2 行だけで、QR-3 の
「この一致は自動テストで検査される」に至っては基準ですらなかった。

FR 側では AC が受け皿になり、設計判断 (「型規則はフィールドの型注釈から導出し、実装と
二重管理にしない」) や責務の境界 (「ラベルを読む倍率の保証はこの表示には求めず、
フォーカス表示が担う」) が溜まっていた。`Decision` を外したときに書いた「解決策は既に
片方の要求の受け入れ基準へ漏れ出しており」と同じ現象が続いていたことになる。

決め手は次の 2 つの数字である。

| | 外す前 |
|---|---|
| FR / QR の受け入れ基準保有率 | **100%** (30/30) |
| `status = verified` のノード | **0 件** |

`structure.missing_acceptance_criteria` は `requirements.py` でも `examples/sample.py` でも
一度も発火していない。100% なのは検査が要求するからで、モデルの健全性を測ってはいなかった。
しかも「空でない」は text の再掲で満たせるので迂回コストがゼロである。一方、テストも
ベンチも CI も揃っていて FR が 20 件 `implemented` に達しているのに `verified` は 0 件の
まま。**「確かめた」と書く場所が無いので、誰も正直に埋められないスロットが放置されて
いた。** 書く前の約束は 100% 強制され、確かめた後の事実は 0% だった。

### 新しい検査が優れている点

発火の時点が**主張の時点**になる。外した検査は、まだ誰も合意していない `proposed` の
要求にまで基準を要求していた。新しい検査は「確かめた」と書いた者にだけ根拠を求める。
検査は 1 本消して 1 本足したので総数は変わらない。

**捨てたもの**: 基準を事前に固定して合意する運用。承認 (`approved`) の対象が text だけに
なる。書き手が 1 人で根拠が手元にある間は損失が出ないが、複数当事者が「何をもって完了と
するか」を交渉する場では効く。`acceptance_criteria` を属性として残したのはこの含みのためで、
QR-4 (「キーボードだけで辿れる」→「tab で到達でき Enter で開ける」) や
`examples/sample.py` のように、text が測定可能に書けないときの操作化としては今も働く。
ただし書かなくても指摘は出ないので、実質的には中心から外れる。

**`evidence` は `Reference` 値にする。** 以前は `list[str]` として始め、集約が必要なら後で昇格させる方針だった。しかし `source` / `realized_by` / `evidence` はいずれも「要求の外側にある情報への参照」であり、`title` / `url` / `note` で表せる構造が共通している。ここを別表現にすると、表示・AST・統計・検索で同じ外部参照を別々に扱う必要が出る。そこで `Reference` を値オブジェクトとして導入し、検証根拠も同じ形に揃えた。

## 外部情報は `Reference` 値としてノードに持たせる

`Source` ノード、`has_source`、`part_of` は外した。外部情報そのものを要求モデル内のノードとして精密にモデル化するのではなく、要求ノードが外部情報へ辿れることを重視する。

```python
Reference(title="顧客ヒアリング", url="https://...", note="CSV 出力要求の直接的な根拠")
```

`note` は引用・要約・コメント・設計者の見解を分けない。分けるほど入力負荷と表示分岐が増える一方、要求モデルとして必要なのは「リンクを開かなくても重要部分と解釈が分かる」ことである。
`url` は任意であり、自然な公開 URL が無い参照では省略する。無関係な Issue やダミー URL で必須性を満たしてはならない。

フィールド名が関係の意味を持つ。

- `Node.source`: なぜこの要求が存在するのか
- `Requirement.realized_by`: どこで・何によってこの要求が実現されているのか
- `Requirement.evidence`: この要求を満たしていると判断できる根拠は何か

`Reference` 自身には `kind=source/evidence/...` を持たせない。同じ値型を、置かれたフィールドで読み分ける。

## 型を分ける基準

**型ごとに異なる構造規則が存在するときだけ型を分ける**。

- FR と QR は分ける。`qualifies` を出せるのは QR のみで、孤立検出の規則も異なる。
- 外部情報は `Reference` 値にする。外部情報は要求間の経路ではなく、要求ノードの属性として読む情報である。
- 引用や条文の一部も `Reference.note` に入れる。同じ条文を複数要求が参照する場合も、集約用の Source ノードではなく、各要求の根拠として明示する。

## Source を図に描かない、ではなく Source をメタモデルから外す

以前は `Source` をノード型として残し、図からだけ隠す判断をしていた。Issue #123 でさらに一段進め、`Source` 自体を要求グラフから外した。

分かれ目は**源泉が経路なのか属性なのか**にある。`refines` や `satisfies` は「この要求はどの要求と繋がっているか」を辿るための経路で、図はそれを読むためにある。源泉は違う。「この要求はどこから来たか」はノード 1 件について読む情報で、他の要求へ渡っていくための経路ではない。経路でないものを経路として描くと、外部参照ハブにレイアウトと近傍探索が引っ張られ、要求どうしの構造が読みにくくなる。

`req graph` / `req explain` / 静的サイトの図は、Goal / Need / FR / QR / Constraint の要求構造に集中する。外部参照は詳細ペインや `req doc` の属性として、`title` / `note` / `url` を表示する。

**捨てたもの**: 図の上で Source をクリックし、その条文に依存する要求を一斉に光らせる操作。代わりに、各要求の詳細で外部参照を直接確認する。集約が必要になったら、要求グラフとは別の検索・一覧機能として足す方が、構造モデルを濁さない。
