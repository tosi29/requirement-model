{
  "name": "requirement-model-site",
  "private": true,
  "version": "0.1.0",
  "description": "req site が生成するページの JS。単体で lint / テストできるようにするためだけの package.json (依存は無い)",
  "type": "module",
  "scripts": {
    "lint": "node --check src/reqmodel/site_logic.js && node --check src/reqmodel/site_app.js",
    "test": "node --test tests/js/*.test.mjs",
    "bench": "node tests/js/bench.mjs"
  }
}
